# Plane.so API credentials
# Get your API key from https://app.plane.so/profile/

# Your Plane.so API key
VITE_PLANE_API_KEY=your_plane_api_key_here

# Your workspace slug (e.g. "my-workspace" from app.plane.so/my-workspace/)
VITE_WORKSPACE_SLUG=your_workspace_slug

# The project selector to pull issues from
# Accepts project ID, identifier (e.g. ARCHIVUM), or project name
VITE_PROJECT_ID=your_project_id_or_identifier_here

# (Optional) Override the local dev server URL used by the capture script
# VITE_DEV_SERVER_URL=http://localhost:5173

# (Optional) Override Plane API host for production builds
# VITE_PLANE_API_BASE_URL=https://api.plane.so

# (Optional) Wallpaper target monitor index on Windows
# - all monitors: all (default), -1, or empty
# - specific monitor: 0, 1, 2, ... (use monitor index order from Windows)
# WALLPAPER_MONITOR_INDEX=0
